spring:
  application:
    name: siniestro-facil-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dmcsiniestrofacil}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
server:
  port: ${PORT:8080}
