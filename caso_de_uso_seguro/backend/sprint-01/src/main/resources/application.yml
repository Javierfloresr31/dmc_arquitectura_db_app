spring:
  application:
    name: siniestro-facil-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dmcsiniestrofacil}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    schemas: siniestro_facil_meta
    default-schema: siniestro_facil_meta
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
security:
  google:
    audience: ${CLOUD_RUN_AUDIENCE:}
    default-role: ${CLOUD_RUN_DEFAULT_ROLE:OPERADOR}
server:
  port: ${PORT:8080}
